@model List<Product> 
@{
    ViewData["Title"] = "Index";
   
}

       <section id="products">
            <div class="container">
                <div class="row" id="productList">
                 @await Component.InvokeAsync("Product",2);
                    
                </div>
                <div class="row justify-content-center">
                    <button class="btn btn-secondary" id="loadMore">LoadMore...</button>
                </div>
            </div>
        </section>

